// Function Hoisting is a JavaScript mechanism where function declarations are moved to the top of their scope during the compilation phase.

console.log(square(5));
function square(num) {
  return num * num;
}
